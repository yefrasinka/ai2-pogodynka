<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Pogodynka{% endblock %}</title>

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

        <style>
            body {
                background: url("https://placehold.co/100x100/FFFFFF/EFEFEF/png?text=53678");
                background-repeat: repeat;
                min-height: 100vh;
            }

            .navbar-brand {
                font-weight: bold;
            }

            footer {
                margin-top: 50px;
                padding: 20px 0;
                text-align: center;
                color: #666;
                border-top: 1px solid #ddd;
            }
        </style>

        {% block stylesheets %}{% endblock %}
    </head>

    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">🌦️ Pogodynka</a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                        data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
                        aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_location_index') }}">Lokalizacje</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_measurement_index') }}">Pomiary</a>
                        </li>
                    </ul>
                </div>

                {% if is_granted('ROLE_USER') %}
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-light ms-3">Logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-light ms-3">Login</a>
                {% endif %}

            </div>
        </nav>

        <main class="container">
            {% block body %}{% endblock %}
        </main>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

        {% block javascripts %}{% endblock %}
    </body>
</html>
